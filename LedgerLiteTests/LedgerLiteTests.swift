import Testing

// Phase 0 placeholder — real test suites added in Phase 1 (Money, cycle math, auto-detection).
struct BuildTests {
    @Test func buildSucceeds() {
        // Passes as long as the test target links successfully against LedgerLite.
        #expect(true)
    }
}
